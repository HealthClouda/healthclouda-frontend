# Environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Next.js build output
.next/
out/

# OS / editor
.DS_Store
.vscode/

# Sensitive internal docs (gitignored — keep locally)
PRD.md
API-doc.md
FRONTEND_HANDOFF.pdf
.vercel

# TypeScript incremental build cache
*.tsbuildinfo

# Playwright generated output (screenshots ARE committed — those live under
# e2e/design/**-snapshots/, this is only the run report/artifacts)
/playwright-report/
/test-results/

# Coverage output (B7) — regenerated by `npm run test:coverage`, never committed.
coverage/
